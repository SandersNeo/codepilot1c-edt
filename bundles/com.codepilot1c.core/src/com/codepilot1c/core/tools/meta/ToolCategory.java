package com.codepilot1c.core.tools.meta;

/**
 * High-level tool categories for routing and filtering.
 */
public enum ToolCategory {
    FILES,
    BSL,
    METADATA,
    FORMS,
    EXTERNAL,
    DCS,
    DIAGNOSTICS,
    EXTENSION,
    WORKSPACE,
    GIT,
    MCP_GENERIC,
    OTHER
}
